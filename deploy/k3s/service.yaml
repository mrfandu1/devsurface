apiVersion: v1
kind: Service
metadata:
  name: devsurface
  labels:
    app: devsurface
spec:
  type: ClusterIP
  selector:
    app: devsurface
  ports:
    - port: 4567
      targetPort: 4567
      protocol: TCP
